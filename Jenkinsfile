pipeline {
    agent any
    stages {
        stage('Local Jenkins File') {
            steps {
                sh 'echo "Testing with local Jenkins file"'
                
            }
        }
    }
}
